

// Module:20.4

/*
Show output from: 1 - 50,
**But there are conditions:
1. If the number is divisible by 3 then instead of the number, show 'foo'
2. If the number is divisible by 5 then instead of the number, show 'chuu'
3. If the number is divisible by both 3 and 5 then instead of the number, show 'fooChuu'
*/

//=======================================================

/*
for (let i = 1; i <= 50; i++){
  
  if (i % 3 === 0 && i % 5 === 0) {
    console.log('fooChuu');
  }
  else if (i % 3 === 0) {
    console.log('fuu');
  }
  else if (i % 5 === 0) {
    console.log('chuu');
  }
  else {
    console.log(i);
  }
}
*/
//=======================================================













